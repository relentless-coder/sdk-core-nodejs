var OAuth = require('../lib/security/oauth/oauth');
var should = require('should');

describe('OAuth', function() {
    it('p12 with wrong alias throws error');
    it('p12 with wrong password throws error');
});